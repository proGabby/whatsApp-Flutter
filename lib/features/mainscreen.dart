import 'package:flutter/material.dart';

class MobileScren extends StatelessWidget {
  const MobileScren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("welcome"),
    );
  }
}
